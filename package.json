{
  "name": "sistema-joyeria",
  "private": true,
  "type": "commonjs",
  "scripts": {
   "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run prisma:generate --prefix backend && npm run prisma:migrate:deploy --prefix backend && npm run build --prefix frontend",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  }
}
